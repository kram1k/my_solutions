import 'dart:io';

void main() {
  var reader = stdin;
  // var writer = stdout;
  String input = reader.readLineSync() ?? '';
  String bedding = reader.readLineSync() ?? '';
  List<String> numbers = input.split(' ');
  int h = int.parse(numbers[0]);
  int n = int.parse(numbers[1]);
  for (var i = 0; i < n; i++){

  }

}