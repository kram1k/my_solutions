import 'dart:io';

void main() {
  String input = stdin.readLineSync() ?? '';
  String bedding = stdin.readLineSync() ?? '';
  List<String> numbers = input.split(' ');
  int h = int.parse(numbers[0]);
  int n = int.parse(numbers[1]);
  for (var i = 0; i < n; i++){

  }

}